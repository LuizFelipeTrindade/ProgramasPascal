Program TestandoConstantes;

const  
 IDADE = integer; 
 CPF = real;

begin 
    read (IDADE);
    read (CPF);
    //Elas não são lidas, pois o programa não reconhece ela como um variavel global
end.

